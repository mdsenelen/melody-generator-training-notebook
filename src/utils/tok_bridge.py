"""Event-token conversion."""
from __future__ import annotations
from typing import List


def events_to_tokens(events: List[int]) -> List[int]:
    return list(events)


def tokens_to_events(tokens: List[int]) -> List[int]:
    return list(tokens)
